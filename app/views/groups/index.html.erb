<h1>Grupos</h1>

<table>
  <tr>
    <td>
        <div align="right">
          <%=button_to 'Crear Grupo', new_group_path %>
        </div>
    </td>
  </tr>
  <tr>
    <td>

    <table border="3">
      <tr>
        <th>Nombre</th>
        <th>Responsable</th>
        <th colspan="3">Acciones</th>
      </tr>

      <% @groups.each do |group| %>
      <tr>
        <td><%=h group.name %></td>
        <td></td> <!--Aca entra el nombre del reponsable de grupo-->
        <td><%= link_to 'Ver', group %></td>
        <td><%= link_to 'Editar', edit_group_path(group) %></td>
        <td><%= link_to 'Eliminar', group, :confirm => 'Esta seguro?', :method => :delete %></td>
      </tr>
    <% end %>
    </table>

    </td>
  </tr>
</table>
<br />